<UserControl
    x:Class="ShipWorks.Products.UI.Import.ImportSucceededStateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ShipWorks.Products.UI.Import"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ShipWorks.UI;component/Styles/BlueSteel/BlueSteelStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style BasedOn="{StaticResource SmallPrimaryButton}" TargetType="Button" />
            <Style BasedOn="{StaticResource FormTextBox}" TargetType="TextBox" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock>Product import succeeded!</TextBlock>
        <TextBlock Grid.Row="1">Results:</TextBlock>
        <StackPanel Grid.Row="2" Margin="24,0,0,0">
            <TextBlock>
                Total products:&#160;<Run Text="{Binding TotalProducts}" /></TextBlock>
            <TextBlock>
                Added:&#160;<Run Text="{Binding AddedProducts}" /></TextBlock>
            <TextBlock>
                Updated:&#160;<Run Text="{Binding UpdatedProducts}" /></TextBlock>
        </StackPanel>

        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button Command="{Binding CloseDialog}">Close</Button>
        </StackPanel>
    </Grid>
</UserControl>
