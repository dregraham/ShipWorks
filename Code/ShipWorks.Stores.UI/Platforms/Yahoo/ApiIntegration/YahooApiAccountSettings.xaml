<UserControl x:Class="ShipWorks.Stores.UI.Platforms.Yahoo.ApiIntegration.YahooApiAccountSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:ShipWorks.UI.Behaviors;assembly=ShipWorks.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:ShipWorks.Stores.UI.Platforms.Yahoo.ApiIntegration"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:valueConverters="clr-namespace:ShipWorks.UI.ValueConverters;assembly=ShipWorks.UI"
             xmlns:yahooValidation="clr-namespace:ShipWorks.Stores.UI.Platforms.Yahoo"
             FontFamily="Tahoma"
             FontSize="11"
             d:DataContext="{d:DesignInstance local:YahooApiAccountSettingsViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">
    <UserControl.Resources>
        <valueConverters:EnumImageConverter x:Key="EnumImage" />
        <valueConverters:VisibleWhenEqualToParameterConverter x:Key="VisibleWhenEqualToParameter" />
        <valueConverters:VisibleWhenEqualToParameterConverter x:Key="VisibleWhenNotEqualToParameter" Invert="true" />
    </UserControl.Resources>
    <StackPanel Margin="24,8,24,8">
        <StackPanel.Resources>
            <Style x:Key="Section" TargetType="{x:Type FrameworkElement}">
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>
        </StackPanel.Resources>
        <TextBlock Style="{StaticResource Section}"><Run Text="Please enter your Yahoo store ID and access token:" />
        </TextBlock>
        <Grid Margin="24,0,24,8">
            <Grid.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Margin" Value="0,0,8,0" />
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="0,2,0,2" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="Padding" Value="2,2,2,2" />
                </Style>
            </Grid.Resources>
            <Grid.Style>
                <StaticResource ResourceKey="Section" />
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0"><Run Text="Store ID:" />
            </TextBlock>
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     MaxLength="50"
                     Text="{Binding YahooStoreID,
                                    UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="1" Grid.Column="0"><Run Text="Access Token:" />
            </TextBlock>
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     MaxLength="200"
                     Text="{Binding AccessToken,
                                    UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center">
                <Run Text="Download orders starting from order number:" />
            </TextBlock>
            <TextBox Grid.Column="1"
                     Margin="8,0,4,0"
                     VerticalAlignment="Center"
                     Padding="2,2,2,2"
                     Text="{Binding BackupOrderNumber,
                                    UpdateSourceTrigger=PropertyChanged}">
                <i:Interaction.Behaviors>
                    <behaviors:TextBoxInputRegExBehavior EmptyValue=""
                                                         MaxLength="19"
                                                         RegularExpression="^-?\d+(,\d+)*(\.\d+(e\d+)?)?(\.)?$" />
                </i:Interaction.Behaviors>
            </TextBox>
            <Image Grid.Column="2"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Source="{Binding IsValid,
                                    Converter={StaticResource EnumImage}}"
                   Stretch="None"
                   Visibility="{Binding IsValid,
                                        Converter={StaticResource VisibleWhenNotEqualToParameter},
                                        ConverterParameter={x:Static yahooValidation:YahooOrderNumberValidation.Validating}}" />

            <Image Grid.Column="2"
                   RenderTransformOrigin=".5,.5"
                   Source="/Shipworks.Core;component/Resources/circle_ball_blue.gif"
                   Stretch="None"
                   Visibility="{Binding IsValid,
                                        Converter={StaticResource VisibleWhenEqualToParameter},
                                        ConverterParameter={x:Static yahooValidation:YahooOrderNumberValidation.Validating}}">
                <Image.RenderTransform>
                    <RotateTransform x:Name="AnimatedRotateTransform" Angle="0" />
                </Image.RenderTransform>
                <Image.Triggers>
                    <EventTrigger RoutedEvent="Control.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation By="10"
                                                 Duration="0:0:1"
                                                 RepeatBehavior="Forever"
                                                 Storyboard.TargetName="AnimatedRotateTransform"
                                                 Storyboard.TargetProperty="Angle"
                                                 To="360" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Image.Triggers>
            </Image>
            <TextBlock Grid.Column="3"
                       Margin="8, 0, 4, 0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="Red"
                       Text="{Binding ValidationErrorMessage}" />
        </Grid>
    </StackPanel>
</UserControl>
