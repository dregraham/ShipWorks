<UserControl x:Class="ShipWorks.Stores.UI.Platforms.Walmart.WizardPages.WalmartStoreSetupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.shipworks.com/ui"
             Background="{x:Static SystemColors.ControlBrush}"
             FontFamily="Tahoma"
             FontSize="11"
             d:DataContext="WalmartStoreSetupControlViewModel"
             d:DesignHeight="500"
             d:DesignWidth="500"
             mc:Ignorable="d">
    <Grid Margin="20 10 10 10">
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Margin" Value="3" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="3" />
                <Setter Property="Padding" Value="2" />
            </Style>
        </Grid.Resources>
        <StackPanel>
            <TextBlock FontWeight="Bold" Text="Account Credentials" />
            <TextBlock Margin="22 4 0 4" Text="Enter your Walmart-issued credentials:" />
            <Grid Margin="35 0 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           HorizontalAlignment="Right"
                           Text="Consumer ID:" />
                <TextBox Grid.Row="0"
                         Grid.Column="1"
                         Text="{Binding ConsumerID,
                                        UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           HorizontalAlignment="Right"
                           Text="Private Key:" />
                <PasswordBox Grid.Row="1"
                             Grid.Column="1"
                             Margin="3"
                             Padding="2">
                    <i:Interaction.Behaviors>
                        <ui:PasswordBoxBindingBehavior Password="{Binding PrivateKey, UpdateSourceTrigger=PropertyChanged}" />
                    </i:Interaction.Behaviors>
                </PasswordBox>
                <TextBlock Grid.Row="2"
                           Grid.Column="0"
                           HorizontalAlignment="Right"
                           Text="Channel Type:" />
                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Text="{Binding ChannelType,
                                        UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="3"
                           Grid.Column="1"
                           Margin="3 0 3 3">
                    <Run>For help adding your Walmart store,</Run>
                    <Hyperlink NavigateUri="http://support.shipworks.com/support/solutions/articles/4000098182-adding-walmart-to-shipworks" ui:Link.OpenInBrowser="true">click here.</Hyperlink>
                </TextBlock>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
