<UserControl x:Class="ShipWorks.Stores.UI.Platforms.Jet.JetStoreSetupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShipWorks.Stores.UI.Platforms.Jet"
             mc:Ignorable="d" 
             d:DesignWidth="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" 
               Grid.Column="0" 
               HorizontalAlignment="Right">API User:</Label>
        <TextBox Grid.Row="0" 
                 Grid.Column="1"
                 Height="23"
                 VerticalContentAlignment="Center"
                 Text="{Binding ApiUser, UpdateSourceTrigger=PropertyChanged}" />
        
        <Label Grid.Row="1" 
               Grid.Column="0"
               HorizontalAlignment="Right">Secret:</Label>
        <TextBox Grid.Row="1" 
                 Grid.Column="1"
                 Height="23"
                 VerticalContentAlignment="Center"
                 Text="{Binding Secret, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Column="1"
                   Grid.Row="2" 
                   TextWrapping="WrapWithOverflow">
            For help finding your API User or Secret, 
            <Hyperlink
                NavigateUri="http://support.shipworks.com/support/solutions/articles/4000106557-adding-jet-com-to-shipworks">
                click here.
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
