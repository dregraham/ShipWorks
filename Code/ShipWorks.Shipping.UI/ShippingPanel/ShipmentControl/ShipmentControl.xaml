<UserControl x:Class="ShipWorks.Shipping.UI.ShippingPanel.ShipmentControl.ShipmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ShipWorks.Shipping.UI"
             xmlns:converters="clr-namespace:ShipWorks.Shipping.UI.ValueConverters"
             xmlns:weight="clr-namespace:ShipWorks.Shipping.UI.ShippingPanel.Weight"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="/ShipWorks.Shipping.UI;component/ShippingPanel/Styles/ServiceControlStyle.xaml"/>
                <ResourceDictionary Source="/ShipWorks.Shipping.UI;component/Styles/EmptyButtonStyle.xaml"/>
                <ResourceDictionary Source="/ShipWorks.Shipping.UI;component/Templates/BlinkingRedErrorTemplate.xaml"/>

            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="RowDefinition" x:Key="SupportsMultiplePackagesRowStyle">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=SupportsMultiplePackages}" Value="True">
                        <Setter Property="RowDefinition.Height" Value="Auto"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=SupportsMultiplePackages}" Value="False">
                        <Setter Property="RowDefinition.Height" Value="0"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="RowDefinition" x:Key="SupportsPackageTypesRowStyle">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=SupportsPackageTypes}" Value="True">
                        <Setter Property="RowDefinition.Height" Value="Auto"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=SupportsPackageTypes}" Value="False">
                        <Setter Property="RowDefinition.Height" Value="0"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Style="{StaticResource SupportsMultiplePackagesRowStyle}" />
                <RowDefinition Style="{StaticResource SupportsMultiplePackagesRowStyle}" />
                <RowDefinition Style="{StaticResource SupportsPackageTypesRowStyle}" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource GridLabel}">Service:</TextBlock>
            <ComboBox Grid.Row="0" Grid.Column="1"
                      ItemsSource="{Binding Services, UpdateSourceTrigger=PropertyChanged}"
                      SelectedValue="{Binding ServiceType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedValuePath="Key"
                      DisplayMemberPath="Value"
                      />

            <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource GridLabel}">Ship date:</TextBlock>
            <DatePicker Grid.Row="1" Grid.Column="1" Width="125" HorizontalAlignment="Left" Margin="4 0 0 0" SelectedDate="{Binding ShipDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0"
                       Style="{StaticResource GridLabel}">Packages:</TextBlock>
            <ComboBox  Grid.Row="2" Grid.Column="1"
                       ItemsSource="{Binding PackageCountList}"
                       SelectedValue="{Binding NumberOfPackages, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <ListView Name="PackagesList" Grid.Row="3" Grid.Column="1" Margin="4 0 0 0"
                      ItemsSource="{Binding PackageAdapters}"
                      SelectedValue ="{Binding Path=SelectedPackageAdapter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Packages (Select to Edit)" Width="Auto"
                                        DisplayMemberBinding="{Binding Index, StringFormat='Package {0}'}"></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <TextBlock Grid.Row="4" Grid.Column="0" Style="{StaticResource GridLabel}">Packaging:</TextBlock>
            <ComboBox Grid.Row="4" Grid.Column="1"
                      ItemsSource ="{Binding PackageTypes, UpdateSourceTrigger=PropertyChanged}"
                      SelectedValue="{Binding Path=SelectedPackageAdapter.PackagingType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMemberPath="Name" />

            <TextBlock Grid.Row="5" Grid.Column="0" Style="{StaticResource GridLabel}">Weight:</TextBlock>

            <weight:WeightInput Grid.Row="5" Grid.Column="1"
                                Margin="0,0,0,0"
                                HorizontalAlignment="Left"
                                Weight="{Binding TotalWeight}"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Style="{StaticResource GridLabel}">Dimensions:</TextBlock>
            <ComboBox Grid.Row="6" Grid.Column="1"/>

            <!--
                Dimensions
            -->
            <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal">
                <TextBox Width="40" Margin="5 0 5 0"/>
                <TextBox Width="40" Margin="5 0 5 0"/>
                <TextBox Width="40" Margin="5 0 5 0"/>
            </StackPanel>

            <!--
                Add Weight
            -->
            <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Style="{StaticResource GridLabel}">Add to weight</TextBlock>
                <CheckBox/>
            </StackPanel>

            <!--
                Insurance
            -->
            <TextBlock Grid.Row="9" Grid.Column="0" Style="{StaticResource GridLabel}">Insurance:</TextBlock>
            <StackPanel  Grid.Row="9" Grid.Column="1" Orientation="Horizontal">
                <CheckBox IsChecked="{Binding Insurance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock/>
            </StackPanel>

            <StackPanel Grid.Row="10" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Style="{StaticResource GridLabel}">Insured Value:</TextBlock>
                <TextBox Width="40"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>