<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ShipWorks.UI.Controls">
    <Style TargetType="{x:Type local:NotificationBorder}">
        <Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NotificationBorder}">
                    <Border BorderThickness="2"
                            CornerRadius="2"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <StackPanel Orientation="Horizontal">
                            <Border BorderThickness="0"
                                    CornerRadius="0"
                                    Margin="0"
                                    VerticalAlignment="Stretch"
                                    MinWidth="30"
                                    Background="{TemplateBinding BorderBrush}" Padding="4,0,8,0">
                                <TextBlock Text="{Binding Path=IconCharacter, RelativeSource={RelativeSource TemplatedParent}}"
                                           Foreground="White"
                                           FontWeight="Bold"
                                           FontSize="24"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>
                            <ContentPresenter Margin="8" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>